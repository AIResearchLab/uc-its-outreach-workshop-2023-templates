# uc-its-outreach-workshop-2023-templates

UC ITS outreach workshop repo 2023 templates

## developing

```bash
# install venv
sudo apt install python3-venv

# make virtual env
python3 -m venv .venv

# source env
source .venv/bin/activate

# install dependencies
pip install -r ./requirements.txt
```
